package riftomer.bcex.gui.container;

import buildcraft.api.transport.pipe.IPipeHolder;
import buildcraft.lib.gui.ContainerPipe;
import net.minecraft.entity.player.EntityPlayer;

public class ContainerDropPipe extends ContainerPipe {
    public ContainerDropPipe(EntityPlayer player, IPipeHolder pipeHolder) {
        super(player, pipeHolder);
    }
}
